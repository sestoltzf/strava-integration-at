import { AppProps, TableProps } from "../types";
import { Table } from "../Table";
import { Glide } from "../Glide";
export declare function tableDefinition(glide: Glide, props: TableProps, appValue?: string, tables?: Table<{}>[]): Promise<string>;
export declare function appDefinition(glide: Glide, props: AppProps): Promise<string>;
